VITE_API_BASE_URL=/api
# Mock is enabled only by Vite development mode, never in production.
# Set VITE_MOCK=false in .env.local for real backend development.
# VITE_MOCK=false
# Tooling credentials: never use a VITE_ prefix.
# YAPI_TOKEN=
# ZENTAO_TOKEN=
